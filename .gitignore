/target
/dev-logs/

# Runtime data (never commit)
OmniDatabase/

# Prompt originals — private, never publish
versions/**/PROMPT.md

# Secrets
.env
.env.*
*.pem

# Dependencies & build output
**/node_modules/
**/dist/
/dist/

# Local debug / bench noise
debug-*.log
omniplayer/perf-bench-last.json

# Local Windows double-click launchers (keep on disk, not in git)
/打开 OmniTrace.bat
/打开 OmniTrace 笔记.bat

# HD screenshot sources (README uses docs/images/)
/readme图片/
