# omni_data MCP

Read-only OmniDatabase tools for external Cursor agents. Tool names align with notes embedded AI (`notes_mcp.rs`).

```bash
npm install && npm run build
OMNI_DATABASE=/path/to/OmniDatabase node dist/index.js
```

Tools: `read_data_root`, `list_recent_cards`, `tail_module_health`.
