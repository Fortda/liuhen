/** 研究资料 — 与 Rust notes_research 对齐 */

export type ResearchMeta = {
  id: string;
  title: string;
  format: string;
  collected_at: number;
  motive: string;
  orig_filename: string;
  rel_dir: string;
};
